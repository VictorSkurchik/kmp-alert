# Copy to server/.env and fill in real values, or export these as shell env vars
# before running `./gradlew :server:run`. server/.env is gitignored - never commit real secrets.
TELEGRAM_BOT_TOKEN=
TELEGRAM_CHAT_ID=
